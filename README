# xmlmparser
Parse an XML file and outputs the results in a readable format

Usage:
  xmlparser <xml input file> [xml input files...] [options]
    -v, --values:        print attribute values only
    -n, --names:         print host names only
    -p, --print:         print without writing to CSV file
    -o=<file name>:      specifies output csv file name
    -{format}:           %h for host name, %p for port, e.g. '-%h:%p'
